-- KMK -->
-- Migration to add library subcategories (folders inside categories)
-- parent_id = 0 means top-level category, otherwise references parent category _id

ALTER TABLE categories ADD COLUMN parent_id INTEGER NOT NULL DEFAULT 0;

CREATE INDEX IF NOT EXISTS idx_categories_parent_id ON categories(parent_id);
-- KMK <--
